[project]
name = "openpi-client"
version = "0.1.0"
requires-python = ">=3.7"
dependencies = [
    "numpy>=1.22.4,<2.0.0",
    "pillow>=9.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 120
target-version = "py37"
